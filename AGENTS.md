# ArrControl Agent Instructions

Read `README.md`, `docs/ASSUMPTIONS.md`, `docs/SRS.md`, `docs/SDD.md`, and `.codex/rules.md` before implementation. Work from `TODO.md` in order unless the user changes priority.

Preserve module boundaries. Never log or return credentials. Never claim a provider is supported without contract evidence. Keep OpenAPI, tests, localization, security notes, and operational docs synchronized with behavior. Use migrations for schema changes. Prefer small vertical slices that build and test.

Do not push, publish images, open PRs, or mutate live Arr/download/media services without explicit authorization.
